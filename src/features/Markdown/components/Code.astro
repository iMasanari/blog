---
import type { HTMLAttributes } from 'astro/types'
import { mergeStyle } from 'molcss'
import * as styles from './Code.styles'

type Props = HTMLAttributes<'code'>

const props = {
  ...Astro.props,
  class: mergeStyle(styles.code, Astro.props.class),
}
---

<code {...props}><slot /></code>
