---
import type { HTMLAttributes } from 'astro/types'
import { tw } from 'tw-tag'

type Props = HTMLAttributes<'h2'>
const props = Astro.props

const blockquoteStyle = tw(`
  relative
  pt-8
  px-4
  pb-2
  rounded
  shadow-neumo-inset
  before:content-['“']
  before:absolute
  before:top-2
  before:left-4
  before:font-serif
  before:text-6xl
  before:[text-shadow:2px_2px_4px_#bebebe,-2px_-2px_4px_#fff]
  before:text-slate-200
`)
---

<blockquote class={blockquoteStyle} {...props}>
  <slot />
</blockquote>
