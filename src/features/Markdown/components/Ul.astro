---
import type { HTMLAttributes } from 'astro/types'
import { tw } from 'tw-tag'

type Props = HTMLAttributes<'ul'>
const props = Astro.props

const ulStyle = tw(`
  list-disc
  pl-10
  my-4
`)
---

<ul class={ulStyle} {...props}>
  <slot />
</ul>
