---
import type { HTMLAttributes } from 'astro/types'
import { mergeStyle } from 'molcss'
import * as styles from './Ul.styles'

type Props = HTMLAttributes<'ul'>

const props = {
  ...Astro.props,
  class: mergeStyle(styles.ul, Astro.props.class),
}
---

<ul {...props}>
  <slot />
</ul>
