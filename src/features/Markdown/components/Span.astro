---
import type { HTMLAttributes } from 'astro/types'
import { createProps } from './Span.logic'

type Props = HTMLAttributes<'span'>

const props = createProps(Astro.props)
---

<span {...props}><slot /></span>
