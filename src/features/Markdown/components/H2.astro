---
import type { HTMLAttributes } from 'astro/types'
import { mergeStyle } from 'molcss'
import * as styles from './H2.styles'

type Props = HTMLAttributes<'h2'>

const props = {
  ...Astro.props,
  class: mergeStyle(styles.h2, Astro.props.class),
}
---

<h2 {...props}>
  <slot />
</h2>
