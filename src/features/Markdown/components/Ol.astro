---
import type { HTMLAttributes } from 'astro/types'
import { tw } from 'tw-tag'

type Props = HTMLAttributes<'ol'>
const props = Astro.props

const olStyle = tw(`
  list-decimal
  pl-10
  my-4
`)
---

<ol class={olStyle} {...props}>
  <slot />
</ol>
