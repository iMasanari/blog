---
import type { HTMLAttributes } from 'astro/types'
import { mergeStyle } from 'molcss'
import * as styles from './Ol.styles'

type Props = HTMLAttributes<'ol'>

const props = {
  ...Astro.props,
  class: mergeStyle(styles.ol, Astro.props.class),
}
---

<ol {...props}>
  <slot />
</ol>
