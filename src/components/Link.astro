---
import type { HTMLAttributes } from 'astro/types'

interface Props extends HTMLAttributes<'a'> {}

const { class: className, ...props } = Astro.props
---

<a class={`${className} text-blue-600 hover:underline`.trim()} {...props}>
  <slot />
</a>
